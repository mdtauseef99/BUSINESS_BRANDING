@model BizzBranding.CommonUtility.EmpBrandingModel

@{
    ViewBag.Title = "EmployeeBrandStatus";
    //Layout = "~/Views/Shared/_Layout.cshtml";
           Layout = "~/Views/Shared/_MasterLayout.cshtml";
     <link href="~/Content/lib/fontawesome/font-awesome.min.css" rel="stylesheet" />
     <link href="~/Content/lib/fontawesome/font-awesome-ie7.min.css" rel="stylesheet" />
     <link href="~/Content/lib/jquery.bxslider/jquery.bxslider.css" rel="stylesheet" />
    <link href="~/Content/style/custom.css" rel="stylesheet" />
}
<section class="brands">
    <div class="container">
       @* <div class="col-sm-2">
            @Html.ActionLink("Create New BusinessNews", "UpdateBusinessNews", "Home", new { @class = "btn btn-sm btn-primary btn-popup-edit" })
        </div>
        <div class="col-sm-4">
            @Html.ActionLink("Upgrade to News Membership", "RenewNewsValidity", "Home", new { @class = "btn btn-sm btn-primary btn-popup-edit" })
        </div>*@
        <div id="tableouterdiv" style="margin-top: 21px;">
            <div id="content-table-inner">
                <div id="table-content">
                    <div class="bs-example">
                        <table class="table">
                            <thead>
                                <tr>
                                    <th class="success">Employee Name</th>
                                    <th class="success">Employee Image</th>
                                    <th class="success">Message</th>
                                      <th class="success">Requested On</th>
                                    <th class="success">Status</th>
                                </tr>
                            </thead>
                            <tbody>

                                @foreach (var item in Model.EmpBrandingList)
                                {
                                    //string str = item.Description;
                                    //if (str.Length > 100)
                                    //{
                                    //    str = str.Substring(0, 200);
                                    //} 
                                    <tr>
                                        <td><b>@item.EmpName</b></td>
                                         <td><img width="150px" height="100px" src="~/Images/EmployeeBrandingImages/@item.EmpImage" alt="..." class="img-thumbnail" style=""></td>
                                        <td>@item.Message</td>
                                        <td>@item.CreateOn</td>
                                         <td class="options-width">
                                       @if (item.IsActive==true)
                                        {
                                           @* @Html.DisplayFor(x=>x.IsActive==true, new {@class="icon-5"})*@
                                             <a href="#" title="Approved"  data-uid="@item.EmpBrandingId" class="icon-5"></a>
                                        }
                                        else
                                        {
                                              @*@Html.DisplayFor(x=>x.IsActive==false, new {@class="inactive"})*@
                                            <a href="#" title="Pending"  data-uid="@item.EmpBrandingId" class="inactive"></a>
                                        }
                                             </td>
                                        @*<td class="options-width">
                                            <a href="@Url.Action("RemoveBusinessNews", "Home", new { @id = item.BusinessNewsId })" title="Delete" class="icon-2 info-tooltip"></a>
                                            <a href="@Url.Action("UpdateBusinessNews", "Home", new { @id = item.BusinessNewsId })" title="Edit" class="icon-1 info-tooltip"></a>

                                        </td>*@
                                    </tr>
                                }
                            </tbody>
                        </table>
                    </div>
                </div>

              @*  <table border="0" cellpadding="0" cellspacing="0" id="paging-table">
                    <tbody>
                        <tr>
                            <td>
                                @{
                            
                                    if (Model.PageID > 0)
                                    {
                                    <a href="@Url.Action("BusinessNewsList", "Home", new { @pid = Model.PageID - 1 })" class="page-left"></a>
                                    }
                                    <div id="page-info">Page <strong>@Model.Current</strong>/ @Model.Pagecount</div>
                                    if (Model.PageID + 1 < Model.Pagecount)
                                    {
                                    <a href="@Url.Action("BusinessNewsList", "Home", new { @pid = Model.PageID + 1 })" class="page-right"></a>
                                    }
                          
                                }

                            </td>
                        </tr>
                    </tbody>
                </table>

                <script>
                    $("a.icon-5,a.inactive").click(function (e) {
                        e.preventDefault();
                        var userid = $(this).attr("data-uid");

                        $.post("/Admin/MasterData/ChangeNewsStatus", { id: userid });

                        $(this).toggleClass('icon-5');
                        $(this).toggleClass('inactive');

                    });

                </script>
                <div class="clear"></div>
                <div class="clear"></div>*@

            </div>
        </div>
    </div>
</section>
