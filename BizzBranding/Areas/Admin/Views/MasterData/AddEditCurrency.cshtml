@model BizzBranding.Areas.Admin.Models.CurrencyWebModel
@{
    ViewBag.Title = "AddEditCurrency";
    Layout = "~/Areas/Admin/Views/Shared/_AdminLayout.cshtml";
    ViewBag.mainmenu = "AddEditCurrency";
}

<div id="page-heading">
    <h1>Add / Edit Currencies</h1>
    @if (Session["Error"] != null)
    {
        <div class="errormsg">
            @Session["Error"].ToString()
        </div>
        Session["Error"] = null;
    }
</div>
@{Html.EnableClientValidation(true);}
@using (Html.BeginForm())
{
    
    @Html.HiddenFor(x => x.CurrencyId)
    <div style="box-shadow: 0 0 5px; border-radius: 10px;">
        <div class="emptyspace"></div>
        
        <div class="row">
            <div ><span class="rowleft">Currency Name</span></div>
            <div class="rowright">
                @Html.TextBoxFor(x => x.CurrencyName, new { @class = "inp-form", @placeholder = "Currency Name" })
                @Html.ValidationMessageFor(x => x.CurrencyName)
            </div>
        </div>
        <div class="row">
            <div class="rowleft">@Html.LabelFor(x => x.IsActive)</div>
            <div class="rowright">
                @Html.CheckBoxFor(x => x.IsActive)
            </div>
        </div>


        <div class="row">
            <div class="rowleft">
                
            </div>
            <div class="rowright">
                <input type="submit" value="Submit" class="form-submit left" />
                <a href="@Url.Action("Currency","MasterData")" class="form-reset">Cancel</a>
            </div>

        </div>
    </div>
}
